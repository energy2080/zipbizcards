module.exports=[];function WPATH(s){var
index=s.lastIndexOf('/'),
path=-1===index?'nl.fokkezb.drawer/'+s:s.substring(0,index)+'/nl.fokkezb.drawer/'+s.substring(index+1);

return 0===path.indexOf('/')?path:'/'+path;
}